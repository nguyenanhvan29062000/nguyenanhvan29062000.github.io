$(document).ready(function(){
    //5%
    link = $('#torrent-link').attr('my-link');
    //80%
    var getLink = getDownLink(link);
    //100%
    window.location(getLink);

})

function getDownLink(link)
{
    //30%

    //50%
}